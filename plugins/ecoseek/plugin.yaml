name: ecoseek
version: "0.6.0"
description: >
  EcoSeek DiDAL Phase 3 — ku-hpc SSH wrapper + EcoAgent tool_server integration.
  Alpha tools: escalate_remote (one-shot), dialectical_exchange (debate loop).
  Beta tools: eco_analyze (EcoAgent tool_server), ku_hpc (SSH wrapper + Slurm fallback).
  Enable via `hermes plugins enable ecoseek`.
author: EcoSeek / alrobles
requires_env: []
hooks: []
